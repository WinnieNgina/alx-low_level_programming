#include <stdio.h>
/**
 * main - Entry point of the program
 *
 * Return: Always 0 (success)
 */
int main(void)
}
	int h;
	int m;

	for (m = '00', h = '00'; m >= 59, h >= 23; m++, h++)
	printf("%d:%d\n", m, h)
	return (0);
}
