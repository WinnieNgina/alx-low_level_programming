#include "main.h"
#include <stdio.h>

/**
 * main - prints the name of the program followed by /n
 * @argc: argument count
 * @argv: argument vector
 * Return: 0 (succes)
 */
int main(int argc, char *argv[]);
{
	void (argc);

	printf("%s\n", argv[0]);
	return (0);
}
